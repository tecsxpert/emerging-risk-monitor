server:
  port: 8082
spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/tooldb
    username: postgres
    password: Sneham@2004

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect